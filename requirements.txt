# --- HTTP surface ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# --- config & schemas ---
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# --- retrieval ---
# No LangChain here: receipts need an exact chunk to citation-tag mapping, and
# owning the 60 lines that do it is simpler than configuring a framework to
# preserve it.
qdrant-client>=1.9,<2.0
fastembed>=0.4,<1.0   # ONNX embeddings and reranking, no torch, no API key

# --- LLM client ---
openai>=1.30,<2.0

# --- misc ---
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0
